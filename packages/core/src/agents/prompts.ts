/** Default: X discusses/analyzes/reviews; should not modify files (product rule, not a hard technical block). */
export const X_DEFAULT_GUIDANCE =
  '[Default] Do not modify workspace files; discuss, analyze, and review only. Unless the user explicitly authorizes file changes in this message.';

/** Appended when the user explicitly allows X to write. */
export const X_WRITE_GUIDANCE = '[User authorized] You may modify workspace files.';

export const C_DEFAULT_GUIDANCE = 'Reply briefly unless the user explicitly asks for detailed analysis.';

export const BRIEF_REPLY = 'Reply briefly unless the user explicitly asks for detailed analysis.';
